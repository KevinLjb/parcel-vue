const types = {
}

const state = {
  msg: 'Welcome to Your Vue.js App'
}

const mutations = {
}

const actions = {
}

const getters = {
  msg: state => {
    return state.msg
  }
}

export default {
  state,
  getters,
  mutations,
  actions
}
